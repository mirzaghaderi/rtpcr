% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ANOVA_DDCt2.R
\name{ANOVA_DDCt2}
\alias{ANOVA_DDCt2}
\title{Delta Delta Ct pairwise comparisons per levels of specified factors}
\usage{
ANOVA_DDCt2(
  x = x,
  numOfFactors,
  numberOfrefGenes,
  specs,
  block,
  mainFactor.level.order = NULL,
  p.adj = "none",
  analyseAllTarget = TRUE,
  model = NULL,
  set_missing_target_Ct_to_40 = FALSE,
  se.type = c("paired.group", "two.group", "single.group"),
  modelBased_se = TRUE
)
}
\arguments{
\item{numOfFactors}{Number of factors in the input data set excluding block.}

\item{specs}{emmeans specification (e.g. "Factor", "A | B")}

\item{block}{Character. Block column name or \code{NULL}.}

\item{p.adj}{p-value adjustment method}

\item{se.type}{Character string specifying how standard error is calculated. 
One of \code{"paired.group"}, \code{"two.group"}, or \code{"single.group"}. 
\code{"paired.group"} computes SE from paired differences (used when a random 
\code{id} effect is present), \code{"two.group"} uses the unpaired two-group 
t-test standard error against the reference level, and \code{"single.group"} 
computes SE within each level using a one-group t-test.}

\item{modelBased_se}{Logical; if TRUE use model residuals, else raw wDCt will be used for computing se.}
}
\value{
Data frame of fold changes and SE for all genes
}
\description{
Performs relative expression (fold change) pairwise comparisons for all
genes per levels of specified factors.

Standard errors are computed from the raw data grouped according to the
\code{specs} argument (same structure used by \pkg{emmeans}).
}
\examples{
ANOVA_DDCt2(data_repeated_measure_1,
    numOfFactors = 1, numberOfrefGenes = 1, model = wDCt ~ time + (1 | id),
    specs = "time", se.type = "paired.group", block = NULL, modelBased_se = T) 
    
ANOVA_DDCt2(data_2factor,
    numOfFactors = 2, numberOfrefGenes = 1,
    specs = "Drought | Genotype", 
    se.type = "single.group", block = NULL) 

}
